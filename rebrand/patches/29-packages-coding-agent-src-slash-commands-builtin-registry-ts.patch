--- a/packages/coding-agent/src/slash-commands/builtin-registry.ts
+++ b/packages/coding-agent/src/slash-commands/builtin-registry.ts
@@ -28,6 +28,7 @@
 	parseModelString,
 } from "../config/model-resolver";
 import { clearPluginRootsAndCaches, resolveActiveProjectRegistryPath } from "../discovery/helpers.js";
+import { td } from "../i18n";
 import { resolveMemoryBackend } from "../memory-backend";
 import { DynamicBorder } from "../modes/components/dynamic-border";
 import { SayknowPetWidget } from "../modes/components/sayknow-pet-widget";
@@ -1652,7 +1653,10 @@
 export const BUILTIN_SLASH_COMMAND_DEFS: ReadonlyArray<BuiltinSlashCommand> = ACTIVE_BUILTIN_SLASH_COMMAND_REGISTRY.map(
 	command => ({
 		name: command.name,
-		description: command.description,
+		// Translated by command name (English description is the fallback source).
+		get description() {
+			return td(`cmd:${command.name}:d`, command.description);
+		},
 		subcommands: command.subcommands,
 		inlineHint: command.inlineHint,
 		priority: command.priority,
