--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -3,7 +3,7 @@
 on:
    push:
       branches: [main]
-      tags: ["v*"]
+      tags: ["v*", "sayknow-v*"]
    pull_request:
       branches: [main]
    workflow_dispatch:
@@ -117,7 +117,7 @@
    # Branch protection must add the always-present `skc-state-gates` status as required.
    skc-state-gates:
       name: skc-state-gates
-      if: ${{ !startsWith(github.ref, 'refs/tags/v') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
+      if: ${{ !(startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/sayknow-v')) && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
       runs-on: ubuntu-22.04
       timeout-minutes: 15
       env:
@@ -146,7 +146,7 @@
    check:
       timeout-minutes: 30
       needs: [relevance]
-      if: ${{ !startsWith(github.ref, 'refs/tags/v') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
+      if: ${{ !(startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/sayknow-v')) && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
       runs-on: ubuntu-22.04
       steps:
          - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
@@ -173,7 +173,7 @@
       timeout-minutes: 60
       needs: [rust-hash, relevance]
       if: ${{ (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) &&
-         (startsWith(github.ref, 'refs/tags/v') || needs.rust-hash.outputs.run-id == '') }}
+         ((startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/sayknow-v')) || needs.rust-hash.outputs.run-id == '') }}
       runs-on: ubuntu-22.04
       strategy:
          fail-fast: false
@@ -191,7 +191,7 @@
               arch: x64
               variant: ${{ matrix.variant }}
               rust_checks: ${{ matrix.rust_checks && 'true' || 'false' }}
-              save_cache: ${{ github.event_name == 'push' && ((github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev') || startsWith(github.ref, 'refs/tags/v')) }}
+              save_cache: ${{ github.event_name == 'push' && ((github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev') || (startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/sayknow-v'))) }}
 
    # Windows-latest smoke for the `bun install -g sayknow-cli` runtime path
    # (issue #525): build the win32-x64 native addon, then exercise the bun-run
@@ -202,7 +202,7 @@
    windows_smoke:
       timeout-minutes: 60
       needs: [relevance]
-      if: ${{ !startsWith(github.ref, 'refs/tags/v') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
+      if: ${{ !(startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/sayknow-v')) && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
       runs-on: windows-latest
       steps:
          - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
@@ -224,7 +224,7 @@
            with:
               shared-key: windows-smoke-win32-x64
               cache-on-failure: true
-              save-if: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')) }}
+              save-if: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || (startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/sayknow-v'))) }}
               cache-workspace-crates: true
          - name: Cache bun dependencies
            if: needs.relevance.outputs.relevant == 'true'
@@ -255,7 +255,7 @@
    native_release:
       timeout-minutes: 90
       needs: [rust-hash]
-      if: ${{ startsWith(github.ref, 'refs/tags/v') }}
+      if: ${{ (startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/sayknow-v')) }}
       strategy:
          fail-fast: false
          matrix:
@@ -274,13 +274,13 @@
               arch: ${{ matrix.arch }}
               variant: ${{ matrix.variant }}
               target: ${{ matrix.target }}
-              save_cache: ${{ github.event_name == 'push' && ((github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev') || startsWith(github.ref, 'refs/tags/v')) }}
+              save_cache: ${{ github.event_name == 'push' && ((github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev') || (startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/sayknow-v'))) }}
 
    test:
       runs-on: ubuntu-22.04
       needs: [native_linux, rust-hash, relevance]
       if: ${{ !cancelled() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) &&
-         !startsWith(github.ref, 'refs/tags/v') && needs.native_linux.result != 'failure' }}
+         !(startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/sayknow-v')) && needs.native_linux.result != 'failure' }}
       timeout-minutes: 60
       steps:
          - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
@@ -336,7 +336,7 @@
    install_methods:
       timeout-minutes: 45
       needs: [relevance]
-      if: ${{ !startsWith(github.ref, 'refs/tags/v') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
+      if: ${{ !(startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/sayknow-v')) && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
       runs-on: ubuntu-22.04
       steps:
          - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
@@ -357,7 +357,7 @@
               shared-key: install-methods-linux-x64
               cache-on-failure: true
               save-if: ${{ github.event_name == 'push' && ((github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev') ||
-                 startsWith(github.ref, 'refs/tags/v')) }}
+                 (startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/sayknow-v'))) }}
               cache-workspace-crates: true
          - name: Cache bun dependencies
            if: needs.relevance.outputs.relevant == 'true'
@@ -376,7 +376,7 @@
 
    release_context:
       name: release context
-      if: ${{ startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch' }}
+      if: ${{ startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/sayknow-v') || github.event_name == 'workflow_dispatch' }}
       runs-on: ubuntu-22.04
       timeout-minutes: 10
       outputs:
@@ -405,7 +405,7 @@
                  sandbox|verify_only|production) ;;
                  *) echo "Invalid release mode: $RELEASE_MODE"; exit 1 ;;
               esac
-              if [[ ! "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
+              if [[ ! "$RELEASE_TAG" =~ ^(sayknow-)?v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
                  if [ "${GITHUB_EVENT_NAME}" = "push" ]; then
                     {
                        echo "stable=false"
@@ -436,7 +436,7 @@
               {
                  echo "mode=$RELEASE_MODE"
                  echo "tag=$RELEASE_TAG"
-                 echo "version=${RELEASE_TAG#v}"
+                 echo "version=${RELEASE_TAG##*v}"
                  echo "source-sha=$source_sha"
                  echo "stable=true"
                  echo "skip-npm=$SKIP_NPM"
@@ -487,7 +487,7 @@
 
    release_binary:
       timeout-minutes: 60
-      if: ${{ always() && startsWith(github.ref, 'refs/tags/v') && !cancelled() &&
+      if: ${{ always() && (startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/sayknow-v')) && !cancelled() &&
          needs.native_linux.result == 'success' && needs.native_release.result ==
          'success' && (needs.check.result == 'success' || needs.check.result == 'skipped') }}
       needs: [check, native_linux, native_release, rust-hash]
