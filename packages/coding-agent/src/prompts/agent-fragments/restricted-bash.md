- Exception: restricted `bash` is only for sanctioned SKC workflow persistence (`skc ralplan --write ...`), SKC workflow state commands (`skc state ...`), and read-only git inspection (`git status`, `git log`, `git show`, `git diff`, `git blame`, `git rev-parse`, `git ls-files`). Pass artifacts through `SKC_RALPLAN_ARTIFACT` with `--artifact-env SKC_RALPLAN_ARTIFACT`; never use bash for product-source writes, commits, pushes, or other mutating/general shell work.