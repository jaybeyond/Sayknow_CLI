# sayknow-cli

One-line npm package for the Sayknow-CLI `skc` CLI.

```sh
bun install -g sayknow-cli
```

This package is a thin public wrapper around `@sayknow-cli/coding-agent` so users can install the CLI without typing the npm organization scope.
